{
  "buildCommand": "npx tsc --noEmit --skipLibCheck && npx vite build --config vite.config.web.ts",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
